name: flutter_location_settings
description: A new Flutter project.
version: 0.0.1
author:
homepage:

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  plugin:
    androidPackage: com.github.droibit.flutter.plugins.flutter_location_settings
    pluginClass: FlutterLocationSettingsPlugin